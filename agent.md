## 注意事项

- 每次改动完成后，都必须创建一个对应的 Git commit，以便后续追踪和回滚。
- 每次改动后，都必须编写或者更新相关测试，并在交付给用户前，确保所有测试和验证全部通过。

## 本仓库的单一事实源约定（重要）

**仓库根就是插件包根**（`package.json` 在根）。DSH 通过 Profile 的 link 依赖
直接指向本目录运行，因此：

- **不存在"另一份要同步的副本"**。改代码就是改这里，改完即生效。
  历史上曾维护两份（插件目录 + 仓库），一改就漂移，已废弃该做法。
- 改完后按顺序做三件事：
  1. `bash install/verify.sh` —— 发布前自检（权限 / 包声明 / 个人化路径 / 密钥 / 语法）。
     它守护的每一项都是真实踩过的坑，**必须全绿**。
  2. 若改了 host 侧代码（`lib/`），需要重启 DSH 才生效；只改 `client/` 由 HMR 热更新。
  3. `git add -A && git commit`，提交信息说明「改了什么 + 为什么」。
  4. 改了影响使用者行为的接口（配置项、路径规则、安装方式）时，
     同步更新 `readme.md`（使用者向）与 `docs/plugin.md`（实现向）。

## 提交信息风格

`<type>: <简述>`，type 取 `feat` / `fix` / `docs` / `chore` / `refactor`。
正文写清根因与影响面，尤其是**会静默出错**的那类改动（写入位置错、配置不生效等）。
