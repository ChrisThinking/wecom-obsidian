# wecom-obsidian

企业微信（WeCom）与 Obsidian 协作相关的工作区。

## 用途

本仓库用于存放该工作区的文档、脚本与配置。

## 目录结构

```
.
└── readme.md    # 仓库说明
```

## 开始使用

```bash
git status          # 查看改动
git add .           # 暂存改动
git commit -m "..." # 提交
```

## 说明

仓库初始由 `git init -b main` 创建，默认分支为 `main`。
